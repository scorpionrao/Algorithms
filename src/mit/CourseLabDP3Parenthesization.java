package main.java;

public class CourseLabDP3Parenthesization {
	
	/*
	 * Sub problems for Strings or Sequences
	 * 
	 * Suffixes - X[i:] - O(n)
	 * 
	 * Prefixes - X[:i] - O(n)
	 * 
	 * Substrings - X[i:j] - O(i;j) - O(n^2)
	 */

}
